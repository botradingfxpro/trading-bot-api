# Remplace ce commentaire par le vrai code Python si tu en as
# Contenu de ton bot original (extrait):
"""
BotFX Pro - Version Serveur (Multi-brokers, Universel)

📌 UTILISATION :
1. Ce bot est fait pour tourner sur VOTRE SERVEUR (Linux, Windows, Mac).
2. Il s'intègre à votre site : vous collectez les infos via un formulaire (clé API, email, mot de passe, etc.)
3. Depuis votre backend, lancez ce fichier avec :
   python3 bot.py --broker_url=https://... --api_key=... --email=... --password=... --symbol=EURUSD

🎯 Le bot applique automatiquement la stratégie :
- Achat si prix < 1.0800
- Vente si prix > 1.1000

✅ Compatible avec tout broker qui a une API REST type Capital.com

📩 Exemple de commande :
python3 bot.py --broker_url=https://demo-api-capital.backend-capital.com --api_key=VOTRECLE --email=EMAIL --password=MDP --symbol=EURUSD

"""